# Do not expose one workspace's React types through pnpm's hidden hoist.
# Web packages use the root React 19.2 types; Expo resolves its local 19.1 types.
hoist-pattern[]=*
hoist-pattern[]=!@types/react
